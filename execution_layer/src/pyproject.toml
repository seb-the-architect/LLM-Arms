[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "local-agent"
version = "0.1.0"
description = "Main 1 package"
requires-python = ">=3.12"
dependencies = ["openai", "requests"]

[tool.setuptools.packages.find]
where = ["src"]
